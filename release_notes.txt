## Bug Fixes

- Calendar exports now include calendar notes
- Calendaria SceneConfig replacement breaking system-specific scene config sheets (Torg Eternity)
- Climate zone format tokens (z/zzzz) returning empty strings
- Festival condition summary not updating after editing the linked festival note
- Festival-backed notes appearing as duplicates in calendar exports
- MiniCal timeskip using stale increment on first skip after changing it
- Pathfinder 1e rests fired by SBC statblock imports advancing world time
- Simple Timekeeping import dropping events on wrong dates due to leap year/intercalary drift
- Weather preset name overrides not included in calendar exports
- Weather unit abbreviations not matching international standards (kph → km/h, mm/hr → mm/h, in/hr → in/h)

## Changes

- New macro-based Simple Timekeeping import flow
- Settings export dialog "Include Notes" toggle removed; notes export with the calendar
