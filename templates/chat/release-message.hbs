<div class="calendaria chat-release">
  <div class="release-header">
    <i class="fa-solid fa-calendar"></i>
    <strong>{{localize "CALENDARIA.Release.Title" version=version}}</strong>
  </div>

  <div class="release-body">
    <p class="release-patreon">
      <strong><a href="https://www.patreon.com/3deathsaves" target="_blank" rel="noopener"><i
            class="fa-brands fa-patreon"></i> Now on Patreon!</a></strong>
    </p>

    <h4>Bug Fixes</h4>
    <ul>
      <li>Custom format strings now accept <code>{literal}</code> alongside <code>[literal]</code> to avoid bracket collisions inside <code>[[ ]]</code> enrichers</li>
      <li>Moon phase anchors on randomized moons with weighted phases land inside the intended phase slice</li>
      <li>Malformed custom calendars or default overrides no longer halt module startup; broken calendars are skipped and logged</li>
    </ul>
  </div>

  <a class="open-note" href="{{repoUrl}}" target="_blank" rel="noopener">
    <i class="fa-solid fa-arrow-up-right-from-square"></i> {{localize "CALENDARIA.Release.ReleaseNotes"}}
  </a>
</div>
